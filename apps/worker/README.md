# Worker (Phase 2+)

Async task worker. Long-running jobs (deep research, scheduled tasks,
file extraction, image generation) run here so the web API never blocks.

Not implemented in Phase 1 — this is an intentional placeholder, not a stub.
